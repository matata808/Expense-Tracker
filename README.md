# Expense-Tracker
A simple expense tracking CLI to manage finances.

## Build and run with Maven

```bash
mvn clean compile
mvn exec:java -Dexec.args="add --description Coffee --amount 4"
```

## Project layout

- `src/main/java` contains the application source code.
- `pom.xml` manages dependencies and build configuration.

